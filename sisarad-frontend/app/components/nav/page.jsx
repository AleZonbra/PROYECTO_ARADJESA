// Route placeholder to avoid exposing the Nav component as a page route.
// The real Nav component lives at ./index.jsx and is dynamically imported by the client wrapper.
export default function RoutePlaceholder() {
  return null;
}